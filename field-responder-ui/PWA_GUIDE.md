# Field Responder - PWA Installation Guide

Your Field Responder app is now a **Progressive Web App (PWA)**! This means users can install it on their phones and use it like a native app.

## 🚀 Features

- **Installable**: Add to home screen on any device
- **Offline Support**: Works without internet connection (after first load)
- **Full Screen**: Runs in standalone mode without browser UI
- **Fast Loading**: Cached resources for instant startup
- **Auto-Updates**: Service worker updates automatically

## 📱 How to Install on Different Devices

### Android (Chrome/Edge)
1. Open the app in Chrome or Edge browser
2. Tap the **three-dot menu** (⋮) in the top right
3. Select **"Add to Home screen"** or **"Install app"**
4. Confirm the installation
5. The app icon will appear on your home screen

### iOS (Safari)
1. Open the app in Safari browser
2. Tap the **Share button** (□↑) at the bottom
3. Scroll down and tap **"Add to Home Screen"**
4. Name the app and tap **"Add"**
5. The app icon will appear on your home screen

### Desktop (Chrome/Edge)
1. Open the app in Chrome or Edge
2. Look for the **install icon** (⊕) in the address bar
3. Click it and confirm installation
4. The app will open in its own window

## 🔧 Development

The PWA is disabled in development mode for easier debugging. To test PWA features:

1. Build the production version:
   ```bash
   npm run build
   npm start
   ```

2. Open `http://localhost:3000` in your browser
3. The service worker and install prompt will be active

## 📦 What's Included

- **Service Worker**: Auto-generated by next-pwa
- **Web Manifest**: `/public/manifest.json` with app metadata
- **App Icons**: Multiple sizes (192x192, 256x256, 384x384, 512x512)
- **Offline Caching**: Automatic caching of static assets
- **Mobile Optimized**: Viewport settings and theme colors

## 🎨 Customization

To customize the PWA:

1. **App Name**: Edit `name` and `short_name` in `/public/manifest.json`
2. **Theme Color**: Update `theme_color` and `background_color` in manifest
3. **Icons**: Replace icon files in `/public/` folder
4. **Caching Strategy**: Modify `next.config.mjs` PWA settings

## 🌐 Browser Support

- ✅ Chrome/Edge (Android & Desktop)
- ✅ Safari (iOS 11.3+)
- ✅ Firefox (Desktop & Android)
- ✅ Samsung Internet
- ✅ Opera

## 📝 Notes

- First visit requires internet connection to load resources
- After installation, the app works offline
- Updates are applied automatically on next visit
- Clear browser cache to force update during development
